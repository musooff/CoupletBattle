<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ballboycorp.battle">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".MyApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".battle.BattleActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".battle.newcouplet.NewCoupletActivity" />
        <activity android:name=".main.home.newbattle.NewBattleActivity" />
        <activity android:name=".splash.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.UserActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".author.AuthorActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".battle.editcouplet.EditCoupletActivity"/>
        <activity android:name=".friendlist.FriendListActivity"/>
        <activity android:name=".main.home.newbattle.choosefriends.ChooseFriendsActivity"/>
        <activity android:name=".battle.invite.InviteActivity"/>
        <activity android:name=".author.newauthor.NewAuthorActivity"/>
    </application>

</manifest>